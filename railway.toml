[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python seed.py && python attack.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
